# 📋 Сравнительный анализ логики страницы /dashboard/issues

**Дата:** 2025-11-02
**Статус:** Анализ текущего состояния vs требуемое состояние
**Страница:** `/dashboard/issues` (Замечания)

---

## 🎯 Требования от пользователя

### Новая бизнес-логика работы с замечаниями:

1. **При создании замечания:**
   - Заполняются все поля модального окна
   - Добавляется фото "До"
   - **НОВОЕ:** После создания замечания на выбранного подрядчика отправляется уведомление на email о новом замечании

2. **Устранение замечания:**
   - **НОВОЕ:** Подрядчик должен устранить замечание в указанный срок
   - **НОВОЕ:** Подрядчик отправляет на проверку через кнопку "Отправить на проверку"
   - **НОВОЕ:** Другие роли (с доступом по матрице) также могут отправлять на проверку

3. **Проверка замечания:**
   - **НОВОЕ:** ИТР или Надзор могут нажать кнопку "Принять"
   - **НОВОЕ:** После нажатия "Принять" замечание переходит в статус "Выполнено" (COMPLETED)
   - **НОВОЕ:** Если замечание не принято, оно переходит на другие статусы автоматически

---

## 📊 Сравнительная таблица: Сейчас vs Должно быть

| № | Функциональность | ✅ Сейчас | 🔄 Должно быть | Статус |
|---|------------------|-----------|----------------|--------|
| 1 | **Создание замечания** | ✅ Работает | ✅ Без изменений | ✅ OK |
| 2 | **Добавление фото "До"** | ✅ Работает | ✅ Без изменений | ✅ OK |
| 3 | **Email уведомление подрядчику** | ❌ **НЕТ** | 🔄 **ДОБАВИТЬ**: При создании замечания отправлять email подрядчику | ❌ ТРЕБУЕТСЯ |
| 4 | **Кнопка "Отправить на проверку"** | ✅ Есть, доступна **всем ролям** | 🔄 **ПРОВЕРИТЬ**: Доступ через матрицу ButtonAccess | ⚠️ ПРОВЕРИТЬ |
| 5 | **Кнопка "Принять"** | ✅ Есть, проверяется через `canAcceptIssue()` | 🔄 **ПРОВЕРИТЬ**: Доступ только ИТР и Надзору | ⚠️ ПРОВЕРИТЬ |
| 6 | **Статус после "Принять"** | ✅ Переходит в COMPLETED | ✅ Без изменений | ✅ OK |
| 7 | **Автоматическое изменение статусов** | ✅ Работает через `get_auto_status()` | ✅ Без изменений | ✅ OK |

---

## 🔍 Детальный анализ текущей реализации

### 1️⃣ Создание замечания (Модальное окно)

#### Текущая реализация (Issues.tsx строки 1161-1359):

**Поля формы:**
- ✅ Название (title) — обязательное
- ✅ Описание (description) — обязательное
- ✅ Проект (project) — обязательное
- ✅ Приоритет (priority) — обязательное (CRITICAL, HIGH, NORMAL)
- ✅ Срок исполнения (deadline) — необязательное
- ✅ Подрядчик (assigned_to) — необязательное
- ✅ Фото "До" (photos_before) — доступно при наличии прав `canAddPhotoBefore()`
- ✅ Фото "После" (photos_after) — доступно при наличии прав `canAddPhotoAfter()`
- ✅ Чекбокс "Принято" — только для ролей ENGINEER, SITE_MANAGER, MASTER

**Обработка (строки 630-651):**
```typescript
const handleModalOk = () => {
  form.validateFields().then((values: any) => {
    const formData = new FormData()
    formData.append('title', values.title)
    formData.append('description', values.description)
    formData.append('project', values.project)
    formData.append('priority', values.priority)
    if (values.deadline) formData.append('deadline', values.deadline.toISOString())
    if (values.assigned_to) formData.append('assigned_to', values.assigned_to)
    if (values.accepted) formData.append('accepted', 'true')

    photosBefore.forEach((file: any) => {
      if (file.originFileObj) formData.append('photos_before', file.originFileObj)
    })
    photosAfter.forEach((file: any) => {
      if (file.originFileObj) formData.append('photos_after', file.originFileObj)
    })

    createIssueMutation.mutate(formData as any)
  })
}
```

**Backend обработка (backend/apps/issues/views.py):**
- ✅ Создает замечание
- ✅ Загружает первое фото "До"
- ✅ Загружает первое фото "После"
- ❌ **НЕТ отправки email подрядчику**

---

### 2️⃣ Кнопка "Отправить на проверку"

#### Текущая реализация (Issues.tsx строки 1054-1083):

**Доступность:**
- ✅ Отображается для **ВСЕХ ролей**, если статус не COMPLETED и не REJECTED
- ❌ **НЕТ проверки через матрицу ButtonAccess**

**Код:**
```typescript
{/* Ряд 3: На проверку (доступно всем ролям, кроме завершенных/отклоненных) */}
{issue.status !== 'COMPLETED' && issue.status !== 'REJECTED' && (
  <Row gutter={8}>
    <Col span={24}>
      <Button
        type={issue.status === 'PENDING_REVIEW' ? 'default' : 'default'}
        size="small"
        icon={<SendOutlined />}
        onClick={() => {
          if (issue.status !== 'PENDING_REVIEW') {
            updateStatusMutation.mutate({ id: issue.id, status: 'PENDING_REVIEW' })
          }
        }}
        block
        disabled={issue.status === 'PENDING_REVIEW'}
      >
        {issue.status === 'PENDING_REVIEW' ? 'На проверке' : 'На проверку'}
      </Button>
    </Col>
  </Row>
)}
```

**Проблема:**
- ❌ Не проверяется доступ через `canUseButton('send_to_review')`
- ✅ Кнопка доступна всем (что может соответствовать требованиям, но нужно уточнить)

---

### 3️⃣ Кнопка "Принять"

#### Текущая реализация (Issues.tsx строки 1085-1110):

**Доступность:**
- ✅ Проверяется через функцию `canAcceptIssue()` (строки 324-332)

**Код проверки доступа:**
```typescript
// Строки 324-332
const canAcceptIssue = () => {
  // SUPERADMIN всегда имеет доступ
  if (user?.is_superuser || user?.role === 'SUPERADMIN') {
    return true
  }
  // Для остальных ролей проверяем через матрицу доступа из админ-панели
  return canUseButton('accept')
}
```

**Проверка в БД:**
```bash
# Проверяем, есть ли кнопка 'accept' в матрице доступа
docker compose exec backend python manage.py shell -c \
  "from apps.core.models import ButtonAccess; \
   btn = ButtonAccess.objects.filter(page='issues', button_key='accept'); \
   print(f'Кнопка accept существует: {btn.exists()}')"
```

**Результат проверки БД:**
❌ **Кнопка `accept` НЕ существует в матрице доступа!**

**Текущее поведение:**
- Функция `canUseButton('accept')` возвращает `false`
- Кнопка "Принять" **НЕ отображается** ни для кого, кроме SUPERADMIN
- **Это БАГ!**

---

### 4️⃣ Автоматическое изменение статусов

#### Текущая реализация (backend/apps/issues/models.py строки 130-168):

**Метод `get_auto_status()`:**

```python
def get_auto_status(self):
    """
    Автоматическое определение статуса замечания на основе правил:
    - До 24 часов с момента создания: "Новое"
    - От создания до окончания срока: "В процессе"
    - После истечения срока: "Просрочено"
    - Если создано Технадзором или Авторским надзором: "На проверке"
    - Если нажали "Принято": "Исполнено" (COMPLETED)
    - Если вручную установлено "На проверке": сохраняем этот статус
    """
    from django.utils import timezone
    from datetime import timedelta

    # Если статус уже COMPLETED, REJECTED или PENDING_REVIEW, не меняем
    if self.status in [self.Status.COMPLETED, self.Status.REJECTED, self.Status.PENDING_REVIEW]:
        return self.status

    now = timezone.now()

    # Проверка: если замечание добавлено от роли "Технадзор" или "Авторский надзор"
    if self.created_by and self.created_by.role in ['SUPERVISOR', 'OBSERVER']:
        return self.Status.PENDING_REVIEW

    # Проверка: просрочено ли замечание
    if self.deadline and now > self.deadline:
        return self.Status.OVERDUE

    # Проверка: новое ли замечание (до 24 часов)
    time_since_creation = now - self.created_at
    if time_since_creation < timedelta(hours=24):
        return self.Status.NEW

    # Если есть срок и он еще не истек - "В процессе"
    if self.deadline and now <= self.deadline:
        return self.Status.IN_PROGRESS

    # По умолчанию - "В процессе"
    return self.Status.IN_PROGRESS
```

**Правила:**
1. ✅ **COMPLETED, REJECTED, PENDING_REVIEW** — не меняются
2. ✅ Создано **SUPERVISOR или OBSERVER** → автоматически **PENDING_REVIEW**
3. ✅ Прошел **deadline** → **OVERDUE**
4. ✅ Меньше **24 часов** с создания → **NEW**
5. ✅ Есть срок и не истек → **IN_PROGRESS**

---

## 🔧 Что нужно изменить/добавить

### ❌ КРИТИЧЕСКИЙ БАГ: Кнопка "Принять" не работает

**Проблема:**
- В базе данных **НЕТ** кнопки с `button_key='accept'`
- Функция `canAcceptIssue()` всегда возвращает `false` (кроме SUPERADMIN)
- **Никто не может принимать замечания!**

**Решение:**
1. Добавить кнопку `accept` в `populate_button_access.py`
2. Указать роли с доступом: ENGINEER, SITE_MANAGER, FOREMAN, MASTER, SUPERVISOR, OBSERVER
3. Запустить команду `populate_button_access`

---

### 🆕 НОВАЯ функциональность: Email уведомления

**Требование:**
> "после добавление замечания, на выбранный подрядчик отправляется уведомление на email о новом замечание"

**Где реализовать:**
- Backend: `backend/apps/issues/views.py` — метод `create()` в ViewSet
- Добавить отправку email после успешного создания замечания

**Что должно быть в email:**
- Название замечания
- Описание
- Проект
- Приоритет
- Срок устранения
- Ссылка на страницу замечания

**Технологии:**
- Django Email (уже используется в проекте)
- Celery для асинхронной отправки (опционально)

---

### ⚠️ ПРОВЕРИТЬ: Доступ к кнопке "Отправить на проверку"

**Текущее состояние:**
- Кнопка доступна **всем ролям**
- **НЕТ проверки через матрицу ButtonAccess**

**Требование:**
> "так же на проверку могут отправить роли у которых открыт доступ (если его нету в матрице, добавь)"

**Решение:**
1. Добавить кнопку `send_to_review` в `populate_button_access.py`
2. Указать роли с доступом (уточнить у пользователя)
3. Изменить код фронтенда (строка 1055):

```typescript
// Было:
{issue.status !== 'COMPLETED' && issue.status !== 'REJECTED' && (

// Должно быть:
{canSendToReview() && issue.status !== 'COMPLETED' && issue.status !== 'REJECTED' && (
```

4. Добавить функцию проверки доступа:

```typescript
const canSendToReview = () => {
  if (user?.is_superuser || user?.role === 'SUPERADMIN') return true
  return canUseButton('send_to_review')
}
```

---

## 📋 Текущее состояние кнопок в матрице ButtonAccess

### Существующие кнопки для страницы `issues`:

| № | button_key | button_name | default_access | Описание |
|---|------------|-------------|----------------|----------|
| 1 | `add_comment` | Добавить комментарий | `True` | ✅ Все роли |
| 2 | `add_photo_after` | Добавить фото ПОСЛЕ | `True` | ✅ Все роли |
| 3 | `add_photo_before` | Добавить фото ДО | `True` | ✅ Все роли |
| 4 | `assign` | Назначить исполнителя | `False` | 🔒 Ограничено |
| 5 | `change_status` | Изменить статус | `False` | 🔒 Ограничено |
| 6 | `create` | Создать замечание | `False` | 🔒 Ограничено |
| 7 | `delete` | Удалить | `False` | 🔒 Ограничено |
| 8 | `edit` | Редактировать | `False` | 🔒 Ограничено |
| 9 | `view_details` | Просмотр деталей | `True` | ✅ Все роли |

### ❌ Отсутствующие кнопки:

| button_key | button_name | Где используется |
|------------|-------------|------------------|
| `accept` | Принять | Issues.tsx:1086 — кнопка "Принять" |
| `send_to_review` | Отправить на проверку | Issues.tsx:1055 — кнопка "На проверку" (опционально) |

---

## 🎯 План действий (в следующем шаге)

### Приоритет 1: Критические баги

1. **Добавить кнопку `accept` в матрицу доступа**
   - Файл: `backend/apps/core/management/commands/populate_button_access.py`
   - Роли с доступом: ENGINEER, SITE_MANAGER, FOREMAN, MASTER, SUPERVISOR, OBSERVER
   - Выполнить: `docker compose exec backend python manage.py populate_button_access`

### Приоритет 2: Новая функциональность

2. **Email уведомления подрядчику**
   - Файл: `backend/apps/issues/views.py`
   - Добавить отправку email в метод `create()`
   - Создать email template
   - Протестировать

### Приоритет 3: Улучшения (опционально)

3. **Добавить кнопку `send_to_review` в матрицу доступа**
   - Уточнить у пользователя, какие роли должны иметь доступ
   - Изменить фронтенд для проверки доступа

---

## 📝 Примечания

### Роли, упомянутые в требованиях:

**ИТР (Инженерно-технические работники):**
- ENGINEER — Инженер ПТО
- SITE_MANAGER — Начальник участка
- FOREMAN — Прораб
- MASTER — Мастер

**Надзор:**
- SUPERVISOR — Технадзор
- OBSERVER — Авторский надзор

**Подрядчик:**
- CONTRACTOR — Подрядчик

**Руководство:**
- DIRECTOR — Директор
- CHIEF_ENGINEER — Главный инженер
- PROJECT_MANAGER — Руководитель проекта

---

## ✅ Итоговая оценка текущей реализации

| Функция | Статус | Описание |
|---------|--------|----------|
| ✅ Создание замечания | **Работает** | Модальное окно, все поля, фото "До" |
| ❌ Email уведомление | **НЕТ** | Требуется добавить |
| ⚠️ Кнопка "Отправить на проверку" | **Работает частично** | Доступна всем, нет проверки матрицы |
| ❌ Кнопка "Принять" | **НЕ РАБОТАЕТ** | Кнопка `accept` отсутствует в БД |
| ✅ Автоматические статусы | **Работает** | Логика `get_auto_status()` корректна |
| ✅ Просмотр замечаний | **Работает** | Фильтры, поиск, карточки |
| ✅ Редактирование | **Работает** | Модальное окно редактирования |
| ✅ Удаление | **Работает** | С тройным подтверждением |
| ✅ Комментарии | **Работает** | Добавление, просмотр, счетчик |
| ✅ Фото | **Работает** | Загрузка, просмотр, удаление |

---

**Дата:** 2025-11-02
**Статус:** ⚠️ Требуется исправление критических багов и добавление новой функциональности
**Следующий шаг:** Ожидание подтверждения от пользователя для начала изменений
