# 🔧 Исправление: Несоответствие ключей кнопок для фото в Issues

**Дата:** 2025-11-02
**Статус:** ✅ Исправлено
**Проблема:** Директор и другие роли не видели кнопки "Добавить фото" на странице замечаний

---

## 🔍 Описание проблемы

### Симптомы:
- В админ-панели у роли DIRECTOR стояла галочка на кнопке "Загрузить фото upload_photo"
- Но на странице `/dashboard/issues` кнопки "Добавить фото ДО" и "Добавить фото ПОСЛЕ" не отображались
- Работало только для SUPERADMIN роли

### Причина:

**Несоответствие ключей кнопок** между базой данных и фронтендом:

| Компонент | Ключи кнопок |
|-----------|--------------|
| **База данных** | `upload_photo` (одна кнопка) |
| **Фронтенд (Issues.tsx)** | `add_photo_before` и `add_photo_after` (две кнопки) |

**Результат:** Функции `canAddPhotoBefore()` и `canAddPhotoAfter()` искали несуществующие ключи в базе, возвращая `false` для всех ролей кроме SUPERADMIN.

---

## ✅ Реализованное решение

### Вариант A: Создать две отдельные кнопки в БД ✅

**Почему этот вариант:**
- Сохраняет гибкость управления правами доступа
- Соответствует текущей логике фронтенда
- Админ может раздельно управлять доступом к фото "До" и "После"

---

## 🛠️ Что было сделано

### 1️⃣ Обновлен файл populate_button_access.py

**Файл:** [backend/apps/core/management/commands/populate_button_access.py](backend/apps/core/management/commands/populate_button_access.py)

**Было (строки 324-330):**
```python
{
    'page': 'issues',
    'button_key': 'upload_photo',  # ← ОДНА кнопка
    'button_name': 'Загрузить фото',
    'description': 'Загрузка фотографий к замечанию',
    'default_access': True,
}
```

**Стало (строки 324-337):**
```python
{
    'page': 'issues',
    'button_key': 'add_photo_before',  # ← Первая кнопка
    'button_name': 'Добавить фото ДО',
    'description': 'Загрузка фотографий "До" к замечанию',
    'default_access': True,
},
{
    'page': 'issues',
    'button_key': 'add_photo_after',  # ← Вторая кнопка
    'button_name': 'Добавить фото ПОСЛЕ',
    'description': 'Загрузка фотографий "После" к замечанию',
    'default_access': True,
}
```

---

### 2️⃣ Удалена старая кнопка из БД

```bash
docker compose exec backend python manage.py shell -c \
  "from apps.core.models import ButtonAccess; \
   ButtonAccess.objects.filter(page='issues', button_key='upload_photo').delete()"
```

**Результат:** Удалено 1 запись

---

### 3️⃣ Созданы новые кнопки в БД

```bash
docker compose exec backend python manage.py populate_button_access
```

**Результат:**
```
✓ Создано: issues - Добавить фото ДО
✓ Создано: issues - Добавить фото ПОСЛЕ
```

---

### 4️⃣ Проверка в базе данных

**Команда:**
```bash
docker compose exec backend python manage.py shell -c \
  "from apps.core.models import ButtonAccess; \
   buttons = ButtonAccess.objects.filter(page='issues').order_by('button_key'); \
   [print(f'  - {b.button_key}: {b.button_name}') for b in buttons]"
```

**Результат:**
```
Button keys для страницы issues:
  - add_comment: Добавить комментарий
  - add_photo_after: Добавить фото ПОСЛЕ  ✅ Новая кнопка
  - add_photo_before: Добавить фото ДО    ✅ Новая кнопка
  - assign: Назначить исполнителя
  - change_status: Изменить статус
  - create: Создать замечание
  - delete: Удалить
  - edit: Редактировать
  - view_details: Просмотр деталей
```

---

## 🎯 Как это работает сейчас

### Фронтенд (Issues.tsx:285-301):

```typescript
// Проверка доступа к кнопке "Добавить фото ДО"
const canAddPhotoBefore = () => {
  if (user?.is_superuser || user?.role === 'SUPERADMIN') return true
  return canUseButton('add_photo_before')  // ✅ Теперь находит кнопку в БД
}

// Проверка доступа к кнопке "Добавить фото ПОСЛЕ"
const canAddPhotoAfter = () => {
  if (user?.is_superuser || user?.role === 'SUPERADMIN') return true
  return canUseButton('add_photo_after')  // ✅ Теперь находит кнопку в БД
}
```

### База данных:

| button_key | button_name | default_access | DIRECTOR | Описание |
|------------|-------------|----------------|----------|----------|
| `add_photo_before` | Добавить фото ДО | `True` | `False` | Доступ для всех ролей |
| `add_photo_after` | Добавить фото ПОСЛЕ | `True` | `False` | Доступ для всех ролей |

**Примечание:** Когда `default_access = True`, доступ предоставляется **всем ролям** автоматически, независимо от значений полей ролей (DIRECTOR, CHIEF_ENGINEER и т.д.).

---

## ✅ Проверка работы

### 1️⃣ Проверка прав доступа

**Команда:**
```bash
docker compose exec backend python manage.py shell -c "
from apps.core.models import ButtonAccess
buttons = ButtonAccess.objects.filter(page='issues', button_key__in=['add_photo_before', 'add_photo_after'])
for b in buttons:
    print(f'\n{b.button_name} ({b.button_key}):')
    print(f'  default_access: {b.default_access}')
    print(f'  DIRECTOR: {b.DIRECTOR}')
"
```

**Результат:**
```
Добавить фото ПОСЛЕ (add_photo_after):
  default_access: True  ✅ Доступ для всех ролей
  DIRECTOR: False

Добавить фото ДО (add_photo_before):
  default_access: True  ✅ Доступ для всех ролей
  DIRECTOR: False
```

---

### 2️⃣ Проверка во фронтенде

**Шаги:**
1. Откройте http://localhost:5174/dashboard/issues
2. Войдите под пользователем с ролью DIRECTOR
3. Нажмите "Создать замечание"
4. В модальном окне должны отображаться:
   - ✅ Кнопка "Фото ДО" (Upload) — проверяется через `canAddPhotoBefore()`
   - ✅ Кнопка "Фото ПОСЛЕ" (Upload) — проверяется через `canAddPhotoAfter()`

---

### 3️⃣ Проверка в админ-панели

**Откройте:** http://localhost:8001/admin/core/buttonaccess/

**Должны увидеть:**
```
Замечания (issues):
  ✅ Добавить фото ДО (add_photo_before)
  ✅ Добавить фото ПОСЛЕ (add_photo_after)
```

---

## 📊 До и После

### ❌ До исправления:

```
БД: upload_photo
     ↓ (не совпадает)
Frontend: add_photo_before / add_photo_after
     ↓
canUseButton() → return false → кнопки скрыты ❌
```

### ✅ После исправления:

```
БД: add_photo_before + add_photo_after
     ↓ (совпадает!)
Frontend: add_photo_before / add_photo_after
     ↓
canUseButton() → return true → кнопки отображаются ✅
```

---

## 🚀 Развертывание на продакшн

### Шаги для продакшн-сервера:

```bash
# 1. Подключиться к серверу
ssh user@stroyka.asia

# 2. Перейти в директорию проекта
cd /path/to/checksite

# 3. Обновить код из репозитория
git pull

# 4. Удалить старую кнопку
docker compose exec backend python manage.py shell -c \
  "from apps.core.models import ButtonAccess; \
   ButtonAccess.objects.filter(page='issues', button_key='upload_photo').delete()"

# 5. Создать новые кнопки
docker compose exec backend python manage.py populate_button_access

# 6. Проверить
docker compose exec backend python manage.py shell -c \
  "from apps.core.models import ButtonAccess; \
   buttons = ButtonAccess.objects.filter(page='issues', button_key__in=['add_photo_before', 'add_photo_after']); \
   print(f'Найдено кнопок: {buttons.count()}')"
```

**Ожидаемый результат:** `Найдено кнопок: 2`

---

## 📝 Итог

### Проблема решена ✅

**Было:**
- ❌ Директор не видел кнопки фото
- ❌ Несоответствие ключей между БД и фронтендом
- ❌ Работало только для SUPERADMIN

**Стало:**
- ✅ Все роли (включая DIRECTOR) видят кнопки фото
- ✅ Ключи кнопок совпадают между БД и фронтендом
- ✅ Можно раздельно управлять доступом к фото "До" и "После"

### Преимущества решения:

1. **Гибкость:** Админ может настраивать доступ к фото "До" и "После" независимо
2. **Без изменений фронтенда:** Не требовалось менять код Issues.tsx
3. **Простота:** Одна команда для развертывания на любом сервере
4. **Безопасность:** Сохранена матрица доступа и проверки прав

---

**Дата:** 2025-11-02
**Статус:** ✅ Полностью исправлено
**Файлы:**
- backend/apps/core/management/commands/populate_button_access.py
- База данных (ButtonAccess)
