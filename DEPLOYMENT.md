# 🚀 Инструкция по деплою изменений

## 📋 Изменения в этом релизе

### 1. Удалена страница "Отчеты" (`/dashboard/reports`)
- ✅ Удален файл `frontend/src/pages/Reports.tsx`
- ✅ Удален файл `frontend/src/api/agile.ts`
- ✅ Удален маршрут из `frontend/src/App.tsx`
- ✅ Удален пункт меню из `frontend/src/components/Layout/MainLayout.tsx`
- ✅ Создана миграция для удаления записей ButtonAccess

### 2. Исправлена проблема с "Внутренними исполнителями"
- ✅ Исправлено сравнение `user.company` и `u.company` (приведение типов)
- ✅ Теперь в продакшене будут отображаться "Внутренние исполнители"

### 3. Добавлено условное логирование WebSocket
- ✅ Логи WebSocket теперь отображаются только в режиме разработки
- ✅ В продакшене логи скрыты (кроме критических ошибок)

---

## 🔧 Шаги для деплоя на продакшен

### Шаг 1: Обновление кода

```bash
# На продакшн сервере
cd /path/to/checksite
git pull origin main
```

### Шаг 2: Применение миграций

```bash
# Применить миграцию для удаления страницы 'reports'
docker compose exec backend python manage.py migrate core

# Вывод должен показать:
# ✅ Удалено X записей ButtonAccess для страницы 'reports'
```

### Шаг 3: Пересборка frontend

```bash
# Пересобрать frontend с новыми зависимостями
docker compose build frontend
docker compose up -d frontend
```

### Шаг 4: Проверка

```bash
# Проверить статистику ButtonAccess
docker compose exec backend python manage.py shell -c "
from apps.core.models import ButtonAccess
print(f'Страниц: {ButtonAccess.objects.filter(access_type=\"page\").count()}')
print(f'Кнопок: {ButtonAccess.objects.filter(access_type=\"button\").count()}')
print(f'Всего: {ButtonAccess.objects.count()}')
"

# Ожидаемый результат:
# Страниц: 13
# Кнопок: 104
# Всего: 117
```

---

## ✅ Проверка после деплоя

1. **Меню навигации**:
   - ❌ Страница "Отчеты" не должна отображаться в меню
   - ✅ Последовательность: Дашборд, Проекты, Персонал, Замечания, Задачи, Заявки, Склад, Тендеры, Настройки

2. **Страница Замечания**:
   - ✅ При добавлении замечания в поле "Исполнитель" должны отображаться:
     - "Подрядчики" (из выбранного проекта)
     - "Внутренние исполнители" (Начальник участка, Прораб, Мастер из той же компании)

3. **Консоль браузера**:
   - ✅ Логи WebSocket **НЕ должны** отображаться в продакшене
   - ✅ В режиме разработки (`npm run dev`) логи должны быть видны

---

## 🔄 Откат изменений (если нужно)

Если возникли проблемы, можно откатить миграцию:

```bash
docker compose exec backend python manage.py migrate core 0007_add_drawing_buttons_to_projects
```

**Примечание**: Откат миграции **НЕ восстанавливает** удаленные записи ButtonAccess для 'reports'.

---

## 📊 Сравнение статистики

| Параметр | До деплоя (продакшн) | После деплоя (ожидается) |
|----------|----------------------|--------------------------|
| Страниц | 14 | 13 |
| Кнопок | 108 | 104 |
| Всего | 122 | 117 |

**Разница**: -5 записей (удалены записи для страницы 'reports')

---

## 📝 Примечания

- Миграция `0008_remove_reports_page_access.py` безопасна и **не влияет** на другие данные
- Удаление страницы 'reports' **необратимо** (откат миграции не восстанавливает записи)
- Файлы TailwindCSS (`tailwind.config.js`, `postcss.config.js`) остаются в проекте, но не используются на других страницах
