# Детальный анализ страницы Projects (/dashboard/projects)

**Дата анализа:** 2025-11-06
**Файл:** `frontend/src/pages/Projects.tsx` (1109 строк)
**Статус:** 🔴 Обнаружены критические проблемы с доступом к кнопкам чертежей

---

## 📋 Краткое резюме

**Основная проблема:** На странице Projects реализованы функции для добавления и удаления чертежей, но в матрице доступа (таблица `ButtonAccess`) **отсутствуют кнопки** `add_drawing` и `delete_drawing`.

**Последствия:**
- ❌ Обычные пользователи (не SUPERADMIN) **НЕ МОГУТ добавлять чертежи** к проектам
- ❌ Обычные пользователи (не SUPERADMIN) **НЕ МОГУТ удалять чертежи** из проектов
- ✅ SUPERADMIN может выполнять эти действия (жёстко закодировано в коде)

---

## 🔍 Детальный анализ

### 1. Текущее состояние кнопок в ButtonAccess

#### ✅ Существующие кнопки для страницы `projects`:

| button_name              | button_key     | description                                       |
| ------------------------ | -------------- | ------------------------------------------------- |
| Создать проект           | create         | Право на создание нового проекта                  |
| Удалить проект           | delete         | Право на удаление проекта                         |
| Редактировать проект     | edit           | Право на редактирование проекта                   |
| Экспорт проектов         | export         | Право на экспорт данных проектов в Excel          |
| Экспорт Excel            | export_excel   | Экспорт проектов в Excel                          |
| Импорт проектов          | import         | Право на импорт данных проектов из Excel          |
| Импорт Excel             | import_excel   | Импорт проектов из Excel                          |
| Просмотр деталей         | view_details   | Просмотр подробной информации о проекте           |
| Проекты                  | (page access)  | Доступ к странице Проекты                         |

#### ❌ Отсутствующие кнопки:

- **add_drawing** - добавление чертежей к проекту
- **delete_drawing** - удаление чертежей из проекта

---

### 2. Код проверки прав доступа

#### Строки 118-135: Функции проверки доступа к чертежам

```typescript
// Функция проверки прав на ДОБАВЛЕНИЕ чертежа через матрицу доступа
const canAddDrawing = () => {
  // SUPERADMIN всегда имеет доступ
  if (user?.is_superuser || user?.role === 'SUPERADMIN') {
    return true
  }
  // Для остальных ролей проверяем через матрицу доступа из админ-панели
  return canUseButton('add_drawing')  // ❌ Кнопка НЕ СУЩЕСТВУЕТ в БД!
}

// Функция проверки прав на УДАЛЕНИЕ чертежа через матрицу доступа
const canDeleteDrawing = () => {
  // SUPERADMIN всегда имеет доступ
  if (user?.is_superuser || user?.role === 'SUPERADMIN') {
    return true
  }
  // Для остальных ролей проверяем через матрицу доступа из админ-панели
  return canUseButton('delete_drawing')  // ❌ Кнопка НЕ СУЩЕСТВУЕТ в БД!
}
```

**Проблема:**
- Функции вызывают `canUseButton('add_drawing')` и `canUseButton('delete_drawing')`
- Эти кнопки **отсутствуют** в таблице `ButtonAccess`
- Следовательно, `canUseButton()` возвращает `false` для всех ролей кроме SUPERADMIN

---

### 3. Места использования функций

#### Строки 876-885: Кнопка "Добавить чертёж" в модальном окне

```typescript
// Кнопка "Добавить чертёж": Директор, Главный инженер, Руководитель проекта, Начальник участка, Инженер, Прораб
...(canAddDrawing() ? [
  <Button
    key="add"
    type="primary"
    icon={<UploadOutlined />}
    onClick={handleAddDrawing}
  >
    Добавить чертёж
  </Button>
] : [])
```

**Комментарий в коде указывает на роли:**
- Директор
- Главный инженер
- Руководитель проекта
- Начальник участка
- Инженер
- Прораб

**Фактически:** Кнопка видна **только SUPERADMIN**.

#### Строки 926-935: Кнопка "Удалить" для чертежа

```typescript
{/* Кнопка "Удалить чертёж": Директор, Главный инженер, Руководитель проекта, Начальник участка, Инженер (БЕЗ Прораба) */}
{canDeleteDrawing() && (
  <Button
    danger
    icon={<DeleteOutlined />}
    onClick={() => handleDeleteDrawing(drawing)}
    size="small"
  >
    Удалить
  </Button>
)}
```

**Комментарий в коде указывает на роли:**
- Директор
- Главный инженер
- Руководитель проекта
- Начальник участка
- Инженер
- **БЕЗ Прораба** (важное отличие!)

**Фактически:** Кнопка видна **только SUPERADMIN**.

---

### 4. Сравнение с другими кнопками (правильная реализация)

#### ✅ Пример правильной реализации (строки 88-115):

```typescript
// Функция проверки прав на ДОБАВЛЕНИЕ объекта через матрицу доступа
const canAddProject = () => {
  if (user?.is_superuser || user?.role === 'SUPERADMIN') {
    return true
  }
  return canUseButton('create')  // ✅ Кнопка 'create' СУЩЕСТВУЕТ в БД
}

const canEditProject = () => {
  if (user?.is_superuser || user?.role === 'SUPERADMIN') {
    return true
  }
  return canUseButton('edit')  // ✅ Кнопка 'edit' СУЩЕСТВУЕТ в БД
}

const canDeleteProject = () => {
  if (user?.is_superuser || user?.role === 'SUPERADMIN') {
    return true
  }
  return canUseButton('delete')  // ✅ Кнопка 'delete' СУЩЕСТВУЕТ в БД
}
```

**Эти функции работают корректно**, потому что кнопки `create`, `edit`, `delete` существуют в таблице `ButtonAccess`.

---

## 🐛 Обнаруженные ошибки

### Ошибка #1: Отсутствие кнопок в матрице доступа

**Серьёзность:** 🔴 Критическая

**Описание:**
- Frontend код ожидает наличие кнопок `add_drawing` и `delete_drawing` в матрице доступа
- Эти кнопки отсутствуют в таблице `core_button_access`
- В результате функциональность недоступна большинству пользователей

**Затронутые роли:**
- DIRECTOR (Директор)
- CHIEF_ENGINEER (Главный инженер)
- PROJECT_MANAGER (Руководитель проекта)
- SITE_MANAGER (Начальник участка)
- ENGINEER (Инженер ПТО)
- FOREMAN (Прораб) - только для добавления

**Обходной путь:**
- В данный момент работает только для SUPERADMIN (жёстко закодировано)

---

### Ошибка #2: Несоответствие комментариев и реализации

**Серьёзность:** 🟡 Средняя

**Описание:**
- В коде присутствуют комментарии с указанием ролей, которые должны иметь доступ
- Фактически эти роли доступа **НЕ РАБОТАЮТ** из-за отсутствия записей в ButtonAccess
- Это вводит в заблуждение разработчиков

**Примеры:**
- Строка 875: `// Кнопка "Добавить чертёж": Директор, Главный инженер, Руководитель проекта, Начальник участка, Инженер, Прораб`
- Строка 925: `// Кнопка "Удалить чертёж": Директор, Главный инженер, Руководитель проекта, Начальник участка, Инженер (БЕЗ Прораба)`

---

## ✅ Функции, работающие корректно

### 1. Управление проектами
- ✅ **Создание проекта** (`canAddProject`) - использует кнопку `create`
- ✅ **Редактирование проекта** (`canEditProject`) - использует кнопку `edit`
- ✅ **Удаление проекта** (`canDeleteProject`) - использует кнопку `delete`

### 2. Excel функции
- ✅ **Скачать шаблон** - доступно всем (строки 775-780)
- ✅ **Импорт Excel** - доступно всем (строки 781-786)
- ✅ **Экспорт Excel** - доступно всем (строки 787-792)

### 3. Просмотр информации
- ✅ **Просмотр чертежей** - работает для всех (строка 273)
- ✅ **Просмотр персонала** - работает для всех (строка 279)
- ✅ **Просмотр подрядчиков** - работает для всех (строка 290)
- ✅ **Открыть PDF чертеж** - работает для всех (строка 418)
- ✅ **Скачать чертёж** - работает для всех (строка 445)

### 4. Мутации данных
- ✅ **createProjectMutation** - работает корректно (строки 171-183)
- ✅ **updateProjectMutation** - работает корректно (строки 186-199)
- ✅ **deleteProjectMutation** - работает корректно (строки 202-212)
- ✅ **deleteDrawingMutation** - работает корректно (строки 215-225)
- ✅ **uploadDrawingMutation** - работает корректно (строки 228-237)
- ✅ **importExcelMutation** - работает корректно (строки 240-271)

### 5. Тройное подтверждение удаления
- ✅ **Удаление проекта** - использует `tripleConfirm` (строки 318-326)
- ✅ **Удаление чертежа** - использует `tripleConfirm` (строки 489-508)

---

## 📝 План реализации

### Этап 1: Создание записей в ButtonAccess (Backend)

Необходимо создать **2 новые записи** в таблице `core_button_access`:

#### 1.1. Кнопка "Добавить чертёж" (add_drawing)

```python
ButtonAccess.objects.create(
    access_type='button',
    company=None,  # Глобальная настройка
    page='projects',
    button_key='add_drawing',
    button_name='Добавить чертёж',
    description='Право на добавление чертежей к проекту в формате PDF',
    default_access=False,
    SUPERADMIN=True,
    DIRECTOR=True,
    CHIEF_ENGINEER=True,
    PROJECT_MANAGER=True,
    SITE_MANAGER=True,
    ENGINEER=True,
    FOREMAN=True,  # ✅ Прораб ИМЕЕТ доступ к добавлению
    MASTER=False,
    SUPERVISOR=False,
    CONTRACTOR=False,
    OBSERVER=False,
    SUPPLY_MANAGER=False,
    WAREHOUSE_HEAD=False,
    SITE_WAREHOUSE_MANAGER=False
)
```

**Роли с доступом:**
- SUPERADMIN ✅
- DIRECTOR ✅
- CHIEF_ENGINEER ✅
- PROJECT_MANAGER ✅
- SITE_MANAGER ✅
- ENGINEER ✅
- FOREMAN ✅

---

#### 1.2. Кнопка "Удалить чертёж" (delete_drawing)

```python
ButtonAccess.objects.create(
    access_type='button',
    company=None,  # Глобальная настройка
    page='projects',
    button_key='delete_drawing',
    button_name='Удалить чертёж',
    description='Право на удаление чертежей из проекта',
    default_access=False,
    SUPERADMIN=True,
    DIRECTOR=True,
    CHIEF_ENGINEER=True,
    PROJECT_MANAGER=True,
    SITE_MANAGER=True,
    ENGINEER=True,
    FOREMAN=False,  # ❌ Прораб НЕ ИМЕЕТ доступа к удалению
    MASTER=False,
    SUPERVISOR=False,
    CONTRACTOR=False,
    OBSERVER=False,
    SUPPLY_MANAGER=False,
    WAREHOUSE_HEAD=False,
    SITE_WAREHOUSE_MANAGER=False
)
```

**Роли с доступом:**
- SUPERADMIN ✅
- DIRECTOR ✅
- CHIEF_ENGINEER ✅
- PROJECT_MANAGER ✅
- SITE_MANAGER ✅
- ENGINEER ✅
- FOREMAN ❌ (БЕЗ доступа - как указано в комментарии кода)

---

### Этап 2: Создание Django Data Migration

Создать миграцию для автоматического добавления записей в БД:

**Файл:** `backend/apps/core/migrations/0XXX_add_drawing_buttons.py`

```python
# Generated manually

from django.db import migrations


def add_drawing_buttons(apps, schema_editor):
    """Добавляет кнопки для работы с чертежами на странице projects"""
    ButtonAccess = apps.get_model('core', 'ButtonAccess')

    # Кнопка: Добавить чертёж
    ButtonAccess.objects.create(
        access_type='button',
        company=None,
        page='projects',
        button_key='add_drawing',
        button_name='Добавить чертёж',
        description='Право на добавление чертежей к проекту в формате PDF',
        default_access=False,
        SUPERADMIN=True,
        DIRECTOR=True,
        CHIEF_ENGINEER=True,
        PROJECT_MANAGER=True,
        SITE_MANAGER=True,
        ENGINEER=True,
        FOREMAN=True,
        MASTER=False,
        SUPERVISOR=False,
        CONTRACTOR=False,
        OBSERVER=False,
        SUPPLY_MANAGER=False,
        WAREHOUSE_HEAD=False,
        SITE_WAREHOUSE_MANAGER=False
    )

    # Кнопка: Удалить чертёж
    ButtonAccess.objects.create(
        access_type='button',
        company=None,
        page='projects',
        button_key='delete_drawing',
        button_name='Удалить чертёж',
        description='Право на удаление чертежей из проекта',
        default_access=False,
        SUPERADMIN=True,
        DIRECTOR=True,
        CHIEF_ENGINEER=True,
        PROJECT_MANAGER=True,
        SITE_MANAGER=True,
        ENGINEER=True,
        FOREMAN=False,  # Прораб НЕ может удалять чертежи
        MASTER=False,
        SUPERVISOR=False,
        CONTRACTOR=False,
        OBSERVER=False,
        SUPPLY_MANAGER=False,
        WAREHOUSE_HEAD=False,
        SITE_WAREHOUSE_MANAGER=False
    )


def remove_drawing_buttons(apps, schema_editor):
    """Откатывает добавление кнопок для чертежей"""
    ButtonAccess = apps.get_model('core', 'ButtonAccess')
    ButtonAccess.objects.filter(
        page='projects',
        button_key__in=['add_drawing', 'delete_drawing']
    ).delete()


class Migration(migrations.Migration):

    dependencies = [
        ('core', '0XXX_previous_migration'),
    ]

    operations = [
        migrations.RunPython(add_drawing_buttons, remove_drawing_buttons),
    ]
```

---

### Этап 3: Применение миграции

```bash
# Создать миграцию
docker compose exec backend python manage.py makemigrations core --empty --name add_drawing_buttons

# Отредактировать файл миграции (добавить код из Этапа 2)

# Применить миграцию
docker compose exec backend python manage.py migrate core
```

---

### Этап 4: Проверка результата

#### 4.1. Проверка через Django Shell

```bash
docker compose exec backend python manage.py shell
```

```python
from apps.core.models import ButtonAccess

# Проверяем наличие кнопок
add_btn = ButtonAccess.objects.filter(page='projects', button_key='add_drawing').first()
delete_btn = ButtonAccess.objects.filter(page='projects', button_key='delete_drawing').first()

print("Кнопка add_drawing:", add_btn)
print("Роли с доступом:", add_btn.get_accessible_roles() if add_btn else None)

print("Кнопка delete_drawing:", delete_btn)
print("Роли с доступом:", delete_btn.get_accessible_roles() if delete_btn else None)
```

**Ожидаемый результат:**

```
Кнопка add_drawing: projects - Добавить чертёж (add_drawing)
Роли с доступом: ['SUPERADMIN', 'DIRECTOR', 'CHIEF_ENGINEER', 'PROJECT_MANAGER', 'SITE_MANAGER', 'ENGINEER', 'FOREMAN']

Кнопка delete_drawing: projects - Удалить чертёж (delete_drawing)
Роли с доступом: ['SUPERADMIN', 'DIRECTOR', 'CHIEF_ENGINEER', 'PROJECT_MANAGER', 'SITE_MANAGER', 'ENGINEER']
```

---

#### 4.2. Проверка через Frontend

1. **Войти как DIRECTOR** (Директор)
   - Открыть страницу `/dashboard/projects`
   - Открыть список чертежей любого проекта
   - ✅ Должна быть видна кнопка "Добавить чертёж"
   - ✅ Должна быть видна кнопка "Удалить" у каждого чертежа

2. **Войти как FOREMAN** (Прораб)
   - Открыть страницу `/dashboard/projects`
   - Открыть список чертежей любого проекта
   - ✅ Должна быть видна кнопка "Добавить чертёж"
   - ❌ НЕ должна быть видна кнопка "Удалить" у чертежей

3. **Войти как CONTRACTOR** (Подрядчик)
   - Открыть страницу `/dashboard/projects`
   - Открыть список чертежей любого проекта
   - ❌ НЕ должна быть видна кнопка "Добавить чертёж"
   - ❌ НЕ должна быть видна кнопка "Удалить" у чертежей

---

### Этап 5: Обновление документации (опционально)

Добавить информацию о новых кнопках в файл `CLAUDE.md`:

```markdown
## Управление чертежами на странице Projects

### Добавление чертежей (add_drawing)
**Доступно:** Директор, Главный инженер, Руководитель проекта, Начальник участка, Инженер, Прораб

### Удаление чертежей (delete_drawing)
**Доступно:** Директор, Главный инженер, Руководитель проекта, Начальник участка, Инженер

**Важно:** Прораб может ДОБАВЛЯТЬ чертежи, но НЕ может их УДАЛЯТЬ.
```

---

## 📊 Статистика страницы

| Параметр                          | Значение           |
| --------------------------------- | ------------------ |
| **Общее количество строк**        | 1109               |
| **Количество компонентов**        | 1 (Projects)       |
| **Количество состояний**          | 7                  |
| **Количество модальных окон**     | 5                  |
| **Количество функций проверки прав** | 5              |
| **Количество мутаций (TanStack Query)** | 6            |
| **Количество queries**            | 2                  |
| **Количество колонок таблицы**    | 7                  |

---

## 🎯 Итоговые рекомендации

### 🔴 Критично - Исправить немедленно

1. **Добавить кнопки в ButtonAccess:**
   - `add_drawing` - с доступом для 7 ролей
   - `delete_drawing` - с доступом для 6 ролей (БЕЗ Прораба)

2. **Создать и применить миграцию:**
   - Data migration для автоматического добавления записей

---

### 🟢 Необязательно - Улучшения

1. **Тестирование:**
   - Протестировать доступ для каждой роли
   - Убедиться, что Прораб не может удалять чертежи

2. **Мониторинг:**
   - Логировать попытки доступа к функциям чертежей
   - Отслеживать использование кнопок

3. **Документация:**
   - Обновить CLAUDE.md с информацией о правах доступа к чертежам

---

## 🔗 Связанные файлы

- **Frontend:** `frontend/src/pages/Projects.tsx` (строки 118-135, 876-885, 926-935)
- **Backend Model:** `backend/apps/core/models.py` (ButtonAccess)
- **Хук доступа:** `frontend/src/hooks/useButtonAccess.ts`
- **API:** `frontend/src/api/projects.ts`

---

## 📝 Заключение

**Страница Projects функционирует корректно**, но имеет **критическую проблему с доступом к управлению чертежами**.

**Причина:** Отсутствие записей в матрице доступа (`ButtonAccess`) для кнопок `add_drawing` и `delete_drawing`.

**Решение:** Создать data migration для добавления двух записей в `core_button_access` с правильной настройкой ролей.

**Время исправления:** ~15 минут

**Риски:** Минимальные, изменения затрагивают только добавление новых записей в БД.

---

**Анализ подготовлен:** Claude (Sonnet 4.5)
**Дата:** 2025-11-06
