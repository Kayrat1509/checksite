# 📊 Отчёт по сравнению локальной и продакшн БД ButtonAccess

**Дата анализа:** 2025-11-04
**Проект:** Check_Site

---

## 🔍 Обнаруженная проблема

В продакшн базе данных **отсутствуют 13 записей** из таблицы `core_buttonaccess` по сравнению с локальной базой данных.

### Статистика:

|                | Локальная БД | Продакшн БД | Разница |
|----------------|--------------|-------------|---------|
| **Кнопки**     | 89           | 77          | **12 кнопок** |
| **Страницы**   | 13           | 12          | **1 страница** |
| **Всего**      | 102          | 89          | **13 записей** |

---

## ❌ Отсутствующие записи

### 📄 Отсутствующая страница (1 запись):

- **ID=148** | `settings/approval-flow` | "Настройки цепочки согласования"

### 🔘 Отсутствующие кнопки (12 записей):

#### Страница: `settings/approval-flow` (4 кнопки)
- **ID=140** | `create` - "Создать цепочку"
- **ID=141** | `edit` - "Редактировать"
- **ID=142** | `delete` - "Удалить"
- **ID=143** | `approve` - "Сохранить изменения"

#### Страница: `material-requests` (6 кнопок)
- **ID=151** | `submit` - "Отправить на согласование"
- **ID=152** | `warehouse_workflow` - "Действия завсклада в workflow"
- **ID=153** | `supply_workflow` - "Действия снабженца в workflow"
- **ID=154** | `director_workflow` - "Действия директора/главного инженера в workflow"
- **ID=155** | `engineer_workflow` - "Действия инженера ПТО в workflow"
- **ID=156** | `pm_workflow` - "Действия руководителя проекта в workflow"

#### Страница: `issues` (1 кнопка)
- **ID=157** | `mark_accepted` - "Отметить принято"

#### Страница: `recycle-bin` (1 кнопка)
- **ID=158** | `clean_expired` - "Очистить просроченные"

---

## 🔧 Решение

### SQL-скрипт для синхронизации

Создан SQL-скрипт для добавления всех 13 отсутствующих записей в продакшн БД:

**Файл:** `backend/scripts/sync_buttonaccess.sql`

### Инструкция по применению:

1. **Создайте backup продакшн базы данных:**
   ```bash
   pg_dump -h <prod-host> -U <prod-user> -d <prod-db> > backup_$(date +%Y%m%d_%H%M%S).sql
   ```

2. **Проверьте подключение к продакшн БД:**
   ```bash
   psql -h <prod-host> -U <prod-user> -d <prod-db>
   ```

3. **Проверьте текущее состояние (до применения):**
   ```sql
   SELECT COUNT(*) FROM core_button_access WHERE company_id IS NULL;
   -- Должно показать: 89
   ```

4. **Примените SQL-скрипт:**
   ```bash
   psql -h <prod-host> -U <prod-user> -d <prod-db> -f backend/scripts/sync_buttonaccess.sql
   ```

5. **Проверьте результат (после применения):**
   ```sql
   SELECT COUNT(*) FROM core_button_access WHERE company_id IS NULL;
   -- Должно показать: 102

   SELECT COUNT(*) FROM core_button_access WHERE company_id IS NULL AND access_type = 'button';
   -- Должно показать: 89

   SELECT COUNT(*) FROM core_button_access WHERE company_id IS NULL AND access_type = 'page';
   -- Должно показать: 13

   SELECT * FROM core_button_access WHERE page = 'settings/approval-flow';
   -- Должно показать: 5 записей (1 страница + 4 кнопки)
   ```

---

## 📋 Что изменится после синхронизации

### 1. Страница "Настройки цепочки согласования" станет доступна

Пользователи с ролями **DIRECTOR**, **CHIEF_ENGINEER**, **PROJECT_MANAGER**, **ENGINEER**, **SITE_MANAGER**, **SUPPLY_MANAGER** получат доступ к странице:
- `/dashboard/settings/approval-flow`

### 2. Появятся кнопки управления цепочкой согласования

На странице настроек появятся кнопки:
- Создать цепочку
- Редактировать
- Удалить
- Сохранить изменения

### 3. Workflow заявок на материалы заработает корректно

Появятся кнопки для контроля workflow по ролям:
- **Снабженец:** Отправка на согласование, управление оплатой и доставкой
- **Завсклад:** Подтверждение отправки со склада
- **Директор/Гл.инженер:** Согласование на финальном этапе
- **Инженер ПТО:** Согласование на своём этапе
- **Руководитель проекта:** Согласование на своём этапе

### 4. Появится кнопка "Отметить принято" для замечаний

Роли **ENGINEER**, **SITE_MANAGER**, **MASTER** смогут отмечать замечания как принятые.

### 5. Появится кнопка "Очистить просроченные" в корзине

Роль **DIRECTOR** сможет очищать просроченные элементы из корзины.

---

## ⚠️ Причины рассинхронизации

Возможные причины, по которым продакшн база отстала от локальной:

1. **Миграция не была запущена на продакшн** - если записи добавлялись через миграцию
2. **Ручное добавление в локальной БД** - записи были добавлены в локальной БД вручную, но не синхронизированы
3. **Откат миграции на продакшн** - миграция была откачена на продакшн, но не в локальной БД
4. **Прямое редактирование через Django Admin** - записи добавлены через админку локально, но не на продакшн

---

## ✅ Проверка успешности

После применения скрипта убедитесь, что:

- [ ] В продакшн БД стало 102 записи (было 89)
- [ ] Страница `/admin/core/buttonaccess/` показывает "13 Страниц (по компаниям)"
- [ ] Страница `/admin/core/buttonaccess/` показывает "89 Кнопок"
- [ ] В продакшн admin-панели видна страница "settings/approval-flow"
- [ ] Пользователи с правами видят страницу "Настройки цепочки согласования" в меню

**Команды для проверки в продакшн:**
```bash
# Подключение к продакшн БД
psql -h <prod-host> -U <prod-user> -d <prod-db>

# Проверка количества записей
SELECT COUNT(*) FROM core_button_access WHERE company_id IS NULL;
-- Ожидается: 102

# Проверка кнопок
SELECT COUNT(*) FROM core_button_access WHERE company_id IS NULL AND access_type = 'button';
-- Ожидается: 89

# Проверка страниц
SELECT COUNT(*) FROM core_button_access WHERE company_id IS NULL AND access_type = 'page';
-- Ожидается: 13

# Проверка settings/approval-flow
SELECT id, access_type, button_key, button_name FROM core_button_access WHERE page = 'settings/approval-flow';
-- Ожидается: 5 записей
```

---

## 📝 Дополнительная информация

**Все 13 отсутствующих записей имеют:**
- `access_type` = 'button' или 'page'
- `company_id` = NULL (глобальные настройки)
- `default_access` = FALSE (доступ по ролям)
- `SUPERADMIN` = TRUE (суперадмин всегда имеет доступ ко всем функциям)

**ID диапазоны:**
- 140-143: settings/approval-flow (кнопки)
- 148: settings/approval-flow (страница)
- 151-156: material-requests (кнопки workflow)
- 157: issues (кнопка)
- 158: recycle-bin (кнопка)

**Структура полей в SQL:**
```sql
INSERT INTO core_button_access (
    id, access_type, company_id, page, button_key, button_name, description,
    default_access, created_at, updated_at,
    "SUPERADMIN", "DIRECTOR", "CHIEF_ENGINEER", "PROJECT_MANAGER", "ENGINEER", "SITE_MANAGER",
    "FOREMAN", "MASTER", "SUPERVISOR", "CONTRACTOR", "OBSERVER",
    "SUPPLY_MANAGER", "WAREHOUSE_HEAD", "SITE_WAREHOUSE_MANAGER"
)
```

**Примечание:** Поле `SUPERADMIN` всегда установлено в `TRUE` для всех записей, что означает, что пользователи с ролью SUPERADMIN имеют полный доступ ко всем функциям системы.

---

**Автор отчёта:** Claude Code
**Контактное лицо:** Kairat Khidirboev
**Проект:** /Users/kairatkhidirboev/Projects/checksite
