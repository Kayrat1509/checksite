# 📘 CLAUDE.md — Проект Check_Site (на основе ТЗ)

## 📋 Общая информация о проекте

**Check_Site** — это комплексная система для управления строительными проектами, замечаниями, проверками и отчетностью. Проект предназначен для автоматизации процессов взаимодействия между заказчиком, подрядчиком, инженером и другими участниками.

---

## 🎯 Основные цели

1. Централизованный учет проектов, участков и замечаний.
2. Автоматизация документооборота и отчетности.
3. Онлайн-мониторинг состояния строительства и устранения дефектов.
4. Повышение прозрачности и эффективности строительного контроля.

---

## ⚠️ ВАЖНЫЕ ПРАВИЛА РАБОТЫ

### 🚫 Запреты
1. **ЗАПРЕЩЕНО использовать git commit** — не создавай коммиты самостоятельно
2. **ЗАПРЕЩЕНО использовать git push** — не отправляй изменения в удалённый репозиторий
3. **ЗАПРЕЩЕНО создавать файлы вне директории** `/Users/kairatkhidirboev/Projects/checksite`

### ✅ Что можно делать
- Читать, редактировать и создавать файлы внутри проекта
- Запускать Docker команды
- Выполнять миграции и Django команды
- Тестировать код локально

---

## 📝 Стиль работы
- Пиши ответы и комментарии на **русском языке**
- Комментируй каждый блок кода на **русском языке**
- Используй команды `docker compose` (версия v2), а не `docker-compose`

---

## 👥 Ролевое управление

### 📊 Полный список ролей (16 ролей)

#### 1️⃣ Руководство (6 ролей)
**Категория:** `MANAGEMENT` | **Права:** `has_full_access=True` | **Доступ:** 13 страниц

| Роль | Код | ИТР | Описание |
|------|-----|-----|----------|
| **Директор** | `DIRECTOR` | ❌ | Руководитель компании, доступ ко всем проектам и аналитике |
| **Главный инженер** | `CHIEF_ENGINEER` | ❌ | Главный технический руководитель |
| **Руководитель проекта** | `PROJECT_MANAGER` | ✅ | Контроль за конкретным проектом, создание/редактирование замечаний |
| **Главный энергетик** | `CHIEF_POWER_ENGINEER` | ✅ | Руководство энергетической службой, работа с замечаниями |
| **Начальник участка** | `SITE_MANAGER` | ✅ | Управление участком строительства |
| **Прораб** | `FOREMAN` | ✅ | Руководство строительными работами на участке |

#### 2️⃣ ИТР и снабжение (6 ролей)
**Категория:** `ITR_SUPPLY` | **Права:** `has_full_access=False` | **Доступ:** 3 страницы

| Роль | Код | ИТР | Описание |
|------|-----|-----|----------|
| **Инженер ПТО** | `ENGINEER` | ✅ | Инженерно-техническое обеспечение |
| **Энергетик** | `POWER_ENGINEER` | ✅ | Специалист по энергетическому оборудованию, работа с замечаниями |
| **Мастер** | `MASTER` | ✅ | Мастер строительных работ |
| **Снабженец** | `SUPPLY_MANAGER` | ❌ | Обеспечение материалами и оборудованием |
| **Зав.Центрсклада** | `WAREHOUSE_HEAD` | ❌ | Заведующий центральным складом |
| **Завсклад объекта** | `SITE_WAREHOUSE_MANAGER` | ❌ | Заведующий складом на объекте |

#### 3️⃣ Надзор и подрядчики (3 роли)

| Роль | Код | ИТР | Описание |
|------|-----|-----|----------|
| **Технадзор** | `SUPERVISOR` | ❌ | Технический надзор за строительством |
| **Подрядчик** | `CONTRACTOR` | ❌ | Подрядная организация |
| **Наблюдатель** | `OBSERVER` | ❌ | Клиент или инспектор (только просмотр) |

#### 4️⃣ Системная роль (1 роль)

| Роль | Код | ИТР | Описание |
|------|-----|-----|----------|
| **Суперадмин** | `SUPERADMIN` | ❌ | Управляет системой и пользователями, все права |

### 🔧 ИТР-роли (7 ролей с правом работы с замечаниями)
Роли, которые могут создавать и проверять замечания (`is_itr=True`):
1. Руководитель проекта
2. **Главный энергетик** ⭐
3. Инженер ПТО
4. Начальник участка
5. Прораб
6. **Энергетик** ⭐
7. Мастер

### 📌 Важная информация о категориях ролей

#### Категория "Руководство" (`MANAGEMENT`)
- **Главный энергетик** (`CHIEF_POWER_ENGINEER`) - входит в категорию "Руководство"
  - `is_management_category = True`
  - `has_full_access = True`
  - Доступ к 13 страницам
  - Может работать с замечаниями (`is_itr = True`)

#### Категория "ИТР и снабжение" (`ITR_SUPPLY`)
- **Энергетик** (`POWER_ENGINEER`) - входит в категорию "ИТР"
  - `is_itr_supply_category = True`
  - `has_full_access = False`
  - Доступ к 3 страницам
  - Может работать с замечаниями (`is_itr = True`)

#### Автоматическая установка прав (сигналы)
При создании или изменении роли пользователя работают сигналы из [backend/apps/users/signals.py](backend/apps/users/signals.py):

1. **`set_first_user_as_owner`** - назначает первого пользователя компании владельцем
2. **`update_full_access_for_management`** - автоматически устанавливает:
   - `has_full_access=True` и `role_category='MANAGEMENT'` для руководящих ролей
   - `has_full_access=False` и `role_category='ITR_SUPPLY'` для ИТР-ролей

---

## 🏗️ Архитектура проекта

### Backend

* **Django 4.2 + DRF** — REST API
* **Channels** — WebSocket уведомления
* **Celery + Redis** — Асинхронные задачи
* **PostgreSQL** — Основная база данных
* **Flower** — Мониторинг Celery
* **Nginx** — Реверс-прокси для сервера

### Frontend

* **React + TypeScript + Vite**
* **Zustand** — управление состоянием
* **Ant Design** — UI-компоненты
* **React Query** — работа с API
* **Leaflet** — отображение карт и геометок
* **PWA** — поддержка офлайн-режима

---

## ⚙️ Функциональные модули

### 1. Пользователи и авторизация

* JWT (access + refresh)
* Ролевая система и разграничение прав
* 2FA (через OTP)

### 2. Проекты и участки

* Создание проектов, участков и категорий
* Привязка пользователей к проектам

### 3. Замечания и статусы

* CRUD замечаний (создание, изменение, удаление)
* Фильтрация по категории, дате, статусу
* Привязка к фото, документам, геометке

### 4. Уведомления (WebSocket)

* Отправка уведомлений в реальном времени
* Отображение в интерфейсе + push-уведомления
* Возможность отключения reconnection при ошибке

### 5. Отчеты и экспорт

* Экспорт PDF и Excel (WeasyPrint, XlsxWriter)
* Ежедневные и ежемесячные отчеты по проекту
* Визуализация прогресса

### 6. Настройки системы

* Управление пользователями и ролями
* Настройка почтовых уведомлений
* Управление интеграциями (WebPush, Email)

---

## 🔒 Безопасность

* CORS, CSRF защита
* rate limiting на уровне API
* Проверка сессий и refresh-токенов
* Ограничение по IP (опционально)

---

## 🧩 Интеграции и API

* **REST API** с документацией (drf-spectacular / Swagger)
* **WebSocket API** — для уведомлений и реального времени
* **Celery API** — асинхронные задачи
* **Admin Panel / Import-Export** — администрирование и загрузка данных

---

## 🧠 Логика обновлений и сборки

* Все сервисы запускаются через `docker compose`
* В режиме разработки изменения кода (backend и frontend) применяются автоматически благодаря volume и `develop.watch`
* Если добавлены новые зависимости или изменены Dockerfile / requirements.txt / package.json — выполнить полную пересборку:

  ```bash
  docker compose down
  docker compose up -d --build

---

### 2️⃣ Название команды
Ты используешь новую версию Docker Compose (v2), поэтому правильная команда теперь:
```bash
docker compose ...

---

## 🐳 Контейнеры и порты 

| Контейнер              | Описание                    | Порт внутри Docker | Порт хоста |
| ---------------------- | --------------------------- | ------------------ | ---------- |
| **checksite_backend**  | Django + API сервер         | 8000               | 8001       |
| **checksite_frontend** | React (Vite) приложение     | 5173               | 5174       |
| **checksite_db**       | PostgreSQL                  | 5432               | 5433       |
| **checksite_redis**    | Redis для Celery и Channels | 6379               | 6379       |
| **checksite_celery**   | Celery воркер               | —                  | —          |
| **checksite_flower**   | Мониторинг Celery задач     | 5555               | 5555       |
| **checksite_nginx**    | Реверс-прокси               | 80                 | 8080       |

---

## 🚀 Запуск проекта

```bash
docker-compose up --build
```

📂 Ограничения по структуре проекта
❗ Запрещается создавать или размещать любые файлы и папки вне директории:
/Users/kairatkhidirboev/Projects/checksite
Вся работа с проектом, включая Docker, frontend и backend, должна выполняться строго внутри этой папки.

Затем открыть:

* Frontend: [http://localhost:5174](http://localhost:5174)
* Backend API: [http://localhost:8001/api/](http://localhost:8001/api/)
* Flower (мониторинг задач): [http://localhost:5555](http://localhost:5555)

---

## 🔐 Система контроля доступа

### Унифицированная матрица доступа ButtonAccess

**Все права доступа** к страницам и кнопкам управляются через админ-панель:
- **URL**: `/admin/core/buttonaccess/`
- **Модель**: `apps.core.models.ButtonAccess`
- **Принцип**: Один источник правды для всех прав доступа

### Типы доступа:

1. **Страницы** (`access_type='page'`)
   - Определяет, видна ли страница в меню
   - Пример: `dashboard`, `projects`, `settings/approval-flow`

2. **Кнопки** (`access_type='button'`)
   - Определяет, доступна ли кнопка/действие на странице
   - Пример: `create`, `edit`, `delete`, `approve`, `export`

### Правила:

✅ **РАЗРЕШЕНО:**
- Изменять права доступа через `/admin/core/buttonaccess/`
- Добавлять новые страницы/кнопки через админку
- Использовать permission классы: `BaseButtonPermission`, `PageAccessPermission`

❌ **ЗАПРЕЩЕНО:**
- Хардкодить проверки ролей в коде (например: `if user.role == 'DIRECTOR'`)
- Создавать альтернативные системы контроля доступа
- Использовать настройки с привязкой к компании (все настройки глобальные, `company=NULL`)

### Логика проверки:

```python
# ✅ Правильно - через ButtonAccess
from apps.core.access_helpers import has_page_access, has_button_access

if has_page_access(user, 'projects'):
    # Пользователь имеет доступ к странице проектов

if has_button_access(user, 'create', 'projects'):
    # Пользователь может создавать проекты

# ❌ НЕПРАВИЛЬНО - хардкод ролей (общий случай)
if user.role == 'DIRECTOR':
    # Такие проверки запрещены для обычных действий!

# ✅ ИСКЛЮЧЕНИЕ: Хардкод ролей РАЗРЕШЕН для бизнес-логики модулей
# Примеры допустимого хардкода ролей:
# 1. Заявки на материалы - только определенные роли могут создавать заявки
if user.role in ['MASTER', 'FOREMAN', 'SITE_MANAGER']:
    # Создание заявки доступно только этим ролям (бизнес-требование)

# 2. Согласование заявок - жесткая цепочка согласования
if user.role == 'SUPPLY_MANAGER':
    # Только снабженец может переводить заявку на оплату (бизнес-требование)

# 3. Любая бизнес-логика, где роль определяет права на специфичные действия
# (не просто CRUD, а сложные бизнес-процессы)
```

### Правило:
- **ButtonAccess** используется для CRUD операций (создать/редактировать/удалить) и доступа к страницам
- **Хардкод ролей** разрешен для бизнес-логики модулей, где роль определяет специфичное поведение в рамках бизнес-процесса

---

## 📋 Структура меню системы (обновлено 2025-11-03)

### Главное меню (последовательность отображения):

1. **Дашборд** — главная панель с общей статистикой
2. **Проекты** — управление строительными объектами (с функцией импорта/экспорта Excel)
3. **Персонал** ⮡ (раскрывающееся меню):
   - Сотрудники — управление персоналом компании
   - Подрядчики — управление подрядными организациями
   - Надзоры — надзорные организации
4. **Замечания** — замечания и дефекты на объектах
5. **Заявки** — заявки на материалы
6. **Склад** — управление складом
7. **Тендеры** — тендерные процедуры
8. **Отчеты** — отчетность по проектам
9. **Настройки** ⮡ (раскрывающееся меню):
   - Настройки системы — общие настройки
   - Цепочка согласования — настройка индивидуальной цепочки согласования заявок

### Изменения в структуре меню:

✅ **Добавлено:**
- Меню "Персонал" с подпунктами (Сотрудники, Подрядчики, Надзоры)
- Меню "Настройки" с подпунктами (Настройки системы, Цепочка согласования)

❌ **Удалено:**
- Техусловия (technical-conditions) — функционал полностью удален из системы

### Матрица доступа:

**Категория "Руководство"** (13 страниц):
- dashboard, projects, issues, users, contractors, supervisions
- material-requests, warehouse, tenders, reports, profile, settings, settings/approval-flow

**Категория "ИТР и снабжение"** (3 страницы):
- dashboard, profile, settings/approval-flow

### Примечания:

- **Все права доступа управляются через** `/admin/core/buttonaccess/`
- Меню динамически формируется на основе матрицы доступа из БД (модель `ButtonAccess`)
- Подменю отображаются только если у пользователя есть доступ хотя бы к одной странице внутри
- На мобильных устройствах меню автоматически сворачивается
- **Все настройки глобальные** - нет отдельных настроек по компаниям
- Хардкод ролей в коде **запрещен** - все проверки идут через ButtonAccess

---
